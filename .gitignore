node_modules/
dist/
build/
coverage/
*.log
.env
.env.local
.DS_Store
.idea/
# Worktrees the agent harness creates inside the checkout. Without this a
# `git add -A` on the parent branch commits an entire second copy of the tree.
.claude/worktrees/
playwright-report/
test-results/
apps/api/test/.database-url
apps/web/dist/
